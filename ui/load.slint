
import { Theme } from "themes/theme.slint";
export component LoadScreen {
    Rectangle {
        background: Theme.background-primary;
        Text {
            text: "Loading...";
            color: Theme.text;
        }    
    }
}