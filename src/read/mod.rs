use mc_classic::{self, Level, classic_to_level};
use mc_classic_js;

use crate::convert::{self, UniversalWorld};

pub fn read_classic_file (path: String) -> UniversalWorld {

    let level: Level = read_classic_file(path);

}